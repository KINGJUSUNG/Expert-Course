1. jsp페이지에서 js파일 집어넣는 태그는 반드시 jQuery넣는 태그 밑에 있어야함
2. url이 이상하게 찍힐 경우 Server 폴더에 server.xml에 맨 밑에 Context태그를 고치면 됌
3. html태그에 데이터를 집어넣고 싶으면 data-"아무이름"=값 넣으면 하나의 태그에 여러 값을 붙일 수 있음
-> 중요한 건 이 때 값을 빼올 때 저 "아무이름"은 반드시 다 소문자로 바꿔서 빼와야한다는 사실
4. 현재 무송이가 넣어준 jquery는 slim 버전이라 ajax 지원이 안됨. 따라서 slim을 지운 풀버전을 따로 등록해주어야 ajax사용가능